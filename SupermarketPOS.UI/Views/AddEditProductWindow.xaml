<Window x:Class="SupermarketPOS.UI.Views.AddEditProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{Binding WindowTitle}" Height="600" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="{Binding WindowTitle}" 
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,24"/>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Product Name -->
                <TextBox Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Product Name *"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,16"/>

                <!-- Barcode -->
                <TextBox Text="{Binding Barcode, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Barcode (optional)"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,8"/>

                <TextBlock Text="Leave empty if product has no barcode" 
                          FontSize="11" Opacity="0.6" Margin="0,0,0,16"/>

                <!-- Short Code -->
                <TextBox Text="{Binding ShortCode, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Short Code (e.g., COC for Coca Cola) *"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,8"
                        CharacterCasing="Upper"
                        MaxLength="10"/>

                <TextBlock Text="Quick code for fast entry at POS (2-10 characters)" 
                          FontSize="11" Opacity="0.6" Margin="0,0,0,16"/>

                <!-- Description -->
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Description"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        AcceptsReturn="True" TextWrapping="Wrap"
                        MinHeight="80" Margin="0,0,0,16"/>

                <!-- Category -->
                <ComboBox ItemsSource="{Binding Categories}"
                         SelectedItem="{Binding SelectedCategory}"
                         DisplayMemberPath="Name"
                         materialDesign:HintAssist.Hint="Category *"
                         Style="{StaticResource MaterialDesignOutlinedComboBox}"
                         Margin="0,0,0,16"/>

                <!-- Unit Price -->
                <TextBox Text="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"
                        materialDesign:HintAssist.Hint="Unit Price *"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,16">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding SaveCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <!-- Cost Price -->
                <TextBox Text="{Binding CostPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"
                        materialDesign:HintAssist.Hint="Cost Price *"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,16"/>

                <!-- Initial Stock -->
                <TextBox Text="{Binding InitialStock, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Initial Stock Quantity"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,16"/>

                <!-- Is Active -->
                <CheckBox IsChecked="{Binding IsActive}"
                         Content="Active Product"
                         Margin="0,0,0,16"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                   HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="CANCEL" 
                   Command="{Binding CancelCommand}"
                   Style="{StaticResource MaterialDesignFlatButton}"
                   Width="100" Margin="0,0,8,0"/>
            <Button Content="SAVE" 
                   Command="{Binding SaveCommand}"
                   Style="{StaticResource MaterialDesignRaisedButton}"
                   Width="100"/>
        </StackPanel>
    </Grid>
</Window>
