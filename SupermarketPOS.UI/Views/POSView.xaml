<Page x:Class="SupermarketPOS.UI.Views.POSView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SupermarketPOS.UI.Views"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1200"
      Title="Point of Sale">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <materialDesign:Card Grid.Column="0" Margin="0,0,8,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBox Grid.Row="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Search or scan barcode" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="16,16,16,8">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding SearchProductCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <TextBlock Grid.Row="1" Text="CATEGORIES" FontWeight="Bold" FontSize="14" Margin="16,8,16,8"/>
                <ItemsControl Grid.Row="2" ItemsSource="{Binding Categories}" Margin="8,0,8,8">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Name}" Command="{Binding DataContext.FilterByCategoryCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="8,4" HorizontalAlignment="Stretch"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Grid.Row="3" Text="PRODUCTS" FontWeight="Bold" FontSize="14" Margin="16,8,16,8" VerticalAlignment="Top"/>
                <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="0,32,0,0">
                    <ItemsControl ItemsSource="{Binding FilteredProducts}" Margin="8">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="8,4" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="12,8">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="13"/>
                                        <TextBlock Text="{Binding UnitPrice, StringFormat=C2}" FontSize="12" Foreground="Green"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </materialDesign:Card>
        <materialDesign:Card Grid.Column="1" Margin="0,0,8,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <DockPanel Margin="16,16,16,8">
                    <TextBlock Text="SHOPPING CART" FontWeight="Bold" FontSize="16" DockPanel.Dock="Left"/>
                    <Button Content="CLEAR" Command="{Binding ClearCartCommand}" Style="{StaticResource MaterialDesignFlatButton}" DockPanel.Dock="Right" Foreground="Red"/>
                </DockPanel>
                <DataGrid Grid.Row="1" ItemsSource="{Binding CartItems}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="16,0,16,16">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding UnitPrice, StringFormat=C2}" Width="Auto"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="60"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding LineTotal, StringFormat=C2}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>
        <materialDesign:Card Grid.Column="2">
            <StackPanel Margin="16">
                <TextBlock Text="ORDER SUMMARY" FontWeight="Bold" FontSize="16" Margin="0,0,0,16"/>
                <Grid Margin="0,0,0,8">
                    <TextBlock Text="Subtotal:"/>
                    <TextBlock Text="{Binding SubTotal, StringFormat=C2}" HorizontalAlignment="Right" FontWeight="Bold"/>
                </Grid>
                <Grid Margin="0,0,0,8">
                    <TextBlock Text="Tax (10%):"/>
                    <TextBlock Text="{Binding TaxAmount, StringFormat=C2}" HorizontalAlignment="Right"/>
                </Grid>
                <Separator Margin="0,8"/>
                <Grid Margin="0,8,0,16">
                    <TextBlock Text="TOTAL:" FontSize="20" FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalAmount, StringFormat=C2}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Right"/>
                </Grid>
                <Separator Margin="0,8,0,16"/>
                <TextBlock Text="PAYMENT" FontWeight="Bold" FontSize="16" Margin="0,0,0,16"/>
                <TextBox Text="{Binding CashReceived, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Cash Received" Style="{StaticResource MaterialDesignOutlinedTextBox}" FontSize="18" Margin="0,0,0,16"/>
                <Grid Margin="0,0,0,16">
                    <TextBlock Text="Change:" FontSize="16" FontWeight="Bold"/>
                    <TextBlock Text="{Binding ChangeAmount, StringFormat=C2}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" Foreground="Green"/>
                </Grid>
                <Button Content="PROCESS PAYMENT" Command="{Binding ProcessPaymentCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Height="50" FontSize="16" Margin="0,0,0,8"/>
                <Button Content="CANCEL" Command="{Binding ClearCartCommand}" Style="{StaticResource MaterialDesignFlatButton}" Height="40" Foreground="Red"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Page>
